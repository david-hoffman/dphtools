# Agentic Harness References

This directory records the sources used to design this repository harness.

`sources.yml` is the manifest. Notes under `notes/` summarize only design-relevant points for this repository. They are not substitutes for the original sources.

Full snapshots are not committed unless license or terms clearly permit it. When a snapshot is committed, the manifest must include its path and SHA-256 checksum.
