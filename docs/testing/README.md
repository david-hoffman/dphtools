# Testing Policy

This project contains scientific and numerical code. Tests must state the behavior they protect and use deterministic data.

Read:

- `fixture-policy.md`
- `numerical-tolerance-policy.md`
- `oracle-policy.md`

For normal local checks, run `make test-fast`. For coverage, run `make coverage`.
